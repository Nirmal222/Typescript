let greetings: string = "Hello Hitesh";

// greetings = 6
// greetings = true
// greetings = {}
// greetings = []
// Typescript safely checks and does not allow the above commented lines to execute as the type of the "greetings" variable is declared as "string"

console.log(greetings)

export {}