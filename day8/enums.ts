// Enums or Enumerators
enum SeatChoice {
     Aisle="aisle",
     Middle="middle",
     Window="window"
}
// enum keys by default assigns themselves values starting from 0
// Howerver we can define our own starting values rest follows
// we an also assign strings but it breaks the CanvasPattern. For more clearance ref enum in video
const nirmalSeat = SeatChoice.Aisle